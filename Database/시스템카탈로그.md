# 시스템 카탈로그(System Catalog)

> "시스템 카탈로그 = DB 구조(테이블, 컬럼, 인덱스, 권한 등)에 대한 정보를 저장하는 메타데이터 저장소"

- 데이터베이스 자체에 대한 정보를 저장하는 특별한 데이터

- 예를 들면, DB 구조와 객체에 대한 정보

- DBMS는 SQL을 실행 할 때, 아래의 정보가 필요함
  
  - 테이블이 존재하는가?

  - 컬럼 이름과 데이터 타입은 무엇인가?

  - PK는 무엇인가?

  - 인덱스가 존재하는가?

  - 사용자는 어떤 정보를 가지고 있는가? 